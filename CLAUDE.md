# CLAUDE.md

The full instructions for this repository live in **`AGENTS.md`**, next to this file, and are
imported here so they load in full:

@AGENTS.md

`AGENTS.md` in turn imports the detailed coding standards from `.claude/rules/`
(`typescript.md`, `tailwind.md`, `astro.md`, `motion.md`, `seo.md`). Read those too — `AGENTS.md`
is deliberately short and defers the detail to them.

This file is a pointer rather than a copy so the two can never drift, and a **real file rather than
a symlink** so it survives being unzipped on a machine that doesn't support them — which is how a
template reaches most buyers.
